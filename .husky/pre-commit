echo "🔍 Running pre-commit checks..."

# Run lint-staged for formatting and basic linting
npx lint-staged

# Quick type check (non-blocking for now - will be enforced in CI)
echo "📝 Type checking (warnings only)..."
npm run type-check || echo "⚠️ TypeScript errors found - will be enforced in CI"
